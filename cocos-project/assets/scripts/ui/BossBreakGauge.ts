import {Node} from 'cc';import {UiFactory} from './UiFactory';export class BossBreakGauge{static create(boss:any){const root=UiFactory.panel('BossBreakGauge',420,64);const max=Math.max(1,Number(boss?.breakMax)||1),cur=Math.max(0,Number(boss?.breakGauge)||0);const label=UiFactory.label(`BREAK  ${cur}/${max}  · 击破 ${boss?.breakCount||0}`,16,'#b9dfff',390,28);label.setPosition(-190,10);root.addChild(label);const bar=UiFactory.panel('BreakFill',Math.max(10,390*cur/max),9);bar.setPosition(-195+Math.max(10,390*cur/max)/2,-18);root.addChild(bar);return root}}