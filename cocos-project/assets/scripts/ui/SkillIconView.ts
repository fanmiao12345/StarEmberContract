import {Node} from 'cc';import {UiFactory} from './UiFactory';import {AssetLoader} from '../assets/AssetLoader';export class SkillIconView{static build(heroId:string,size=54){const n=UiFactory.panel('SkillIcon',size,size,undefined,Math.round(size/2));const l=UiFactory.label('✦',Math.round(size*.42),undefined,size,size);l.setPosition(-size/2+4,0);n.addChild(l);(n as any).assetKey=AssetLoader.skill(heroId);return n}}
