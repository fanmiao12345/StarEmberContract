import {Node} from 'cc';import {UiFactory} from './UiFactory';export class FormationRowPanel{static create(formation:string[],rows:Record<string,string>,onToggle:(id:string)=>void){const root=UiFactory.panel('FormationRowPanel',620,210);['back','front'].forEach((row,ri)=>{const title=UiFactory.label(row==='front'?'FRONT 前排':'BACK 后排',14,'#9aa6ca',110,28);title.setPosition(-290,55-ri*95);root.addChild(title);formation.filter(id=>(rows[id]||'back')===row).forEach((id,i)=>{const b=UiFactory.button(id,90,54,()=>onToggle(id),false);b.setPosition(-130+i*105,55-ri*95);root.addChild(b)})});return root}}