import type {AssetManifest} from './AssetManifest';export class AssetLoader{private static _manifest:AssetManifest|null=null;static setManifest(m:AssetManifest){this._manifest=m}static get manifest(){return this._manifest}static hero(id:string){return this._manifest?.heroes?.[id]||null}static skill(id:string){return this.hero(id)?.skill||`ui/skills/${id}`}static status(id:string){return this._manifest?.status?.[id]||`ui/status/${id}`}static boss(stage:number){return this._manifest?.bosses?.[String(stage)]||`ui/bosses/stage-${stage}`}static bossSplash(stage:number){return this._manifest?.bossSplash?.[String(stage)]||this.boss(stage)}static release(){return this._manifest?.release||null}static background(id:string){return this._manifest?.backgrounds?.[id]||`ui/backgrounds/${id}`}}
