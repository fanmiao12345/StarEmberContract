# 星烬契约 v2.0.7 活动 / 事务所 / 商店专项检查

## 本轮目标
将 v2.0.6 之后仍然偏网页长页的低频系统，统一为竖屏手游的“顶部摘要 + 分类 Tab + 当前玩法单屏内容”结构。

## 已修复
1. **活动页过长**：月蚀回廊、活动 Boss、星港夜航、羁绊不再同时纵向展开，改为四个可切换模块。
2. **活动关卡在小屏过度压缩**：手机改为横向滑动关卡卡片，首通、推荐战力、奖励与挑战状态集中显示。
3. **活动兑换信息分散**：月蚀商店与夜航补给改为当前活动下的折叠抽屉。
4. **活动 Boss 存在开发文案**：移除 “PROTOTYPE / 原型” 玩家可见表达，改成正式讨伐 UI。
5. **商店仍像后台列表**：新增星髓钱包、今日总限购进度、资源缺口推荐与紧凑购买卡。
6. **玩家界面出现 ART DIRECTION 开发卡片**：从正式商店页移除。
7. **事务所仍然是任务/邮件/成就/账号长页堆叠**：拆分为 今日 / 目标 / 邮件 / 账号 四个 Tab。
8. **待领取信息不明显**：事务所顶部和“更多系统”入口增加待领取数量提示。
9. **版本元数据不一致**：v2.0.6 的 build-web-v20.mjs 仍硬编码 2.0.5；v2.0.7 改为读取 web/version.json 自动生成 release-info.json。
10. **CONFIG_VERSION 未随 UI 版本推进**：更新为 `2026.09.v20.7`，SAVE_VERSION 仍为 20，无需迁移玩家存档。

## 回归覆盖
新增 v2.0.7 UI smoke coverage：四类活动切换、商店正式 UI、事务所分类、开发文案隐藏。

## 部署影响
仅 Web 前端、PWA 缓存与版本元数据变化。CloudBase 数据库结构和现有云函数接口均未修改。

## 验证结果
- `npm test`：全部通过（含 v2.0.4 / v2.0.5 / v2.0.6 / v2.0.7 回归）。
- `npm run check`：44 个脚本语法检查通过。
- `npm run release:check`：75 个资源、10 个发布文件、0 缺失。
- `npm run release:build`：构建成功，`release-info.json` 正确写入 2.0.7 / 2026.09.v20.7。
