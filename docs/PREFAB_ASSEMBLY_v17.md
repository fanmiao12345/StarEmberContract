# Cocos Prefab v17
BattleRoot 按 `Battle_v17.json` 装配：BossBreakGauge、前后排、HeroSkillBar、LiveSkillCommandPanel、ComboBanner、BattleStatsPanel。Formation 使用 `Formation_v17.json`。经济与战斗结果仍只通过 GameApi/CloudBase 写入。
