# Cocos Prefab Assembly v1.8

新增 `BattlePrefabAssembler` 与 `HomePrefabAssembler`，允许在 Creator 3.8.x 的空场景节点中运行时建立标准骨架。正式美术 Prefab 仍建议由 Creator 编辑器保存；JSON 蓝图用于结构约束和自动检查。

Battle_v18: BreakBurstWindow / ClassCounterBadge / ComboSignatureFx。
Home_v18: DynamicBackground / HeroStage / ChapterCard / QuickActions。
