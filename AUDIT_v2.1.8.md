# 星烬契约 v2.1.8 Full Mobile UI Rebuild

## 背景
v2.1.7 已确认 CloudBase 正确返回新版 `index.html` 与内容哈希资源，但实际视觉仍接近旧版本。进一步对比 v2.1.5→v2.1.7 代码后确认，上一轮主要重做顶部栏、首页、底部导航与 CSS 覆盖，探索/编队/养成/契约/活动/事务所主体仍大量沿用旧 DOM，因此用户感知不明显。

## 本轮原则
本轮不再继续给旧长页面换皮，而是把核心页面改为真正的手机信息架构：

- Web 容器最大宽度固定为 460px；桌面访问也以“手机游戏窗口”呈现。
- 底部保持 5 大主入口：首页 / 探索 / 编队 / 活动 / 事务所。
- 子页面采用聚焦式 Tab，单次只展示一类核心任务，减少纵向堆叠。
- 主要操作保持在屏幕下半区，并统一 46px+ 的触控高度。
- 保留 v2.1.5 全部玩法、经济、存档与 CloudBase 规则。

## 关键结构变化

### 首页
继续使用大厅式构图：角色主视觉、今日星轨、继续主线、扫荡/培养、右侧快捷入口、每日/每周进度。

### 探索
由“所有内容一起往下排”改为三个模式：

1. 主线章节：章节主视觉、当前战力、主线挑战、章节路线、战斗策略。
2. 资源裂境：只显示资源副本与今日剩余次数。
3. 扫荡 / 记录：只显示扫荡、战斗复盘、录像。

任务直达会自动切换到对应模式，例如资源任务进入资源裂境，扫荡任务进入扫荡记录。

### 编队
顶部直接展示队伍战力与共鸣状态；阵容区域和候选角色区分层显示，候选契灵改为更适合手机的卡片网格。

### 契灵养成
角色页改为：属性 / 升级 / 天赋 / 装备 / 羁绊。升级与天赋不再和完整图鉴、装备等同时纵向展开。

### 契约召唤
保留限定/常驻、保底、概率、单抽/十连逻辑，强化卡池主视觉和底部抽卡操作区。

### 活动
保留月蚀回廊 / Boss / 夜航 / 羁绊四 Tab，但整体收敛到 460px 手机容器，并提高活动卡片与按钮触控区域。

### 事务所
今日 / 目标 / 邮件 / 账号继续四 Tab，奖励操作区更接近屏幕底部，避免旧式长页堆叠。

## 发布与缓存
- Web version: 2.1.8
- SAVE_VERSION: 20（不变）
- CONFIG_VERSION: 2026.09.v21.5（不变）
- Service Worker cache: `star-ember-v218-full-ui`
- 正式构建继续采用内容哈希文件名。

本次构建核心资源：
- `styles.3c624cebd0.css`
- `app.433d269d88.js`

## 验证
- `npm test`: 全部通过
- `npm run check`: 116 files, cloud helper drift 0
- `npm run release:check`: 75 assets / 10 release files / 0 missing
- `npm run release:build`: 成功

部署后可访问 `/release-probe-2.1.8.json` 确认线上版本。
