# 星烬契约 v2.0.8 移动端品质检查与修复报告

## 本轮目标

v2.0.4-v2.0.7 已经完成阵容、战斗、契约、养成、整备、活动、商店和事务所的第一轮手游化。本轮不新增大系统，而是处理“能玩但还不像正式手游”的共性问题：大厅信息密度、红点反馈、页面切换、加载状态、错误提示、空状态、弹层一致性，以及 360/390/430px 竖屏细节。

## 发现并修复的问题

### 1. 首页缺少一眼可读的日常状态
首页虽然已经有快捷入口，但玩家仍需要进入事务所、活动页或账号页才能知道“有没有奖励可领、活动还剩多少次、当前是不是云存档”。v2.0.8 新增三格状态坞：

- 星轨事务所：直接显示待处理数量。
- 限时活动：显示月蚀、Boss、夜航剩余次数合计。
- 云存档：显示已连接 / 本地中 / 本地试玩，并可直接连接或刷新。

### 2. 重要状态缺少手游式红点
新增轻量提示：

- 顶部“更多”显示事务所/活动综合通知。
- 契约：契灵印达到十连时提示 `10`。
- 阵容：未满 5 人时提示 `!`。
- 契灵：存在未使用星痕点时提示 `!`。
- 探索：已通关且仍有扫荡次数时显示剩余次数。

提示只做导航，不修改任何玩法数值。

### 3. 异步操作的全屏反馈过于网页化
旧版只有“同步中…”全屏文字，且成功/失败没有明确视觉区分。现在：

- 使用紧凑星轨加载卡，保留阻止重复点击但减少空白感。
- 错误 Toast 使用独立错误态，并延长至约 3.2 秒。
- 普通提示和成功/联网恢复提示使用不同视觉层级。
- 浏览器离线/恢复联网会给出明确反馈。

### 4. 云存档提醒侵入所有页面
CloudBase 已配置但未连接时，旧版提示会在契灵、阵容、战斗等所有页面顶部出现。v2.0.8 只在首页保留提醒；账号页继续保留完整云存档信息。

### 5. 页面与按钮缺少统一触控反馈
补齐：

- 页面短促淡入/位移动画。
- 按钮按压缩放和亮度反馈。
- `focus-visible` 键盘焦点。
- `prefers-reduced-motion` 时关闭非必要动效。

### 6. 空状态与弹层风格不一致
- 通用空状态增加星轨标记、虚线边框和一致背景。
- 设置、首次契约、战斗结算、Boss 入场等弹层统一边框/阴影/移动端按钮布局。
- 360px 小屏下结算按钮不会因为三按钮横排而被挤压。

### 7. 小屏宽度仍存在“同一个 600px 规则覆盖所有手机”的问题
新增三档细化：

- `<=360px`：进一步压缩顶栏、资源条、首页主视觉与主线卡；状态坞改为 2+1 布局。
- `361-400px`：针对 390px 主流设备控制首页高度和横滑卡宽度。
- `401-450px`：430px 设备适当放大角色主视觉，同时减少无意义留白。

## 版本与后端兼容性

- Web 版本：`2.0.8`
- `CONFIG_VERSION = 2026.09.v20.8`
- `SAVE_VERSION = 20`
- PWA Cache：`star-ember-v20-mobile8`
- CloudBase ENV：`star-ember-d2grc7dhvba07314f`

没有修改数据库结构、存档版本或云函数调用协议，因此本轮只需要重新发布 Web 静态资源。

## 回归验证

执行：

```text
npm test
npm run check
npm run release:check
npm run release:build
```

通过项目：

- v2.0 core tests
- v2.0 UI smoke
- v2.0 UI feature smoke
- v2.0 visual UI
- v2.0.4 mobile system regression
- v2.0.5 battle mobile UX regression
- v2.0.6 contract/growth/gear regression
- v2.0.7 liveops/utility/shop regression
- v2.0.8 mobile quality polish regression
- release asset test
- syntax check：44 files
- release assets：75 assets / 10 release files / 0 missing

## 下一阶段建议

v2.0.9 应开始处理“正式试玩最后一公里”：首次进入与回流玩家路径、PWA 安装提示、资源预加载/弱网降级、异常恢复、云端连接状态恢复，以及基于真实手机截图逐页做视觉 QA。
