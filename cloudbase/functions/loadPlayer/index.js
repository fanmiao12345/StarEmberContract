const game=require('./lib/game'); const c=require('./lib/cloud');
exports.main=async()=>{try{const {db,auth}=c.init();const ctx=await c.playerContext(db,auth);game.ensureDaily(ctx.state);ctx.state.lastLogin=game.nowIso();const payload={...ctx.state};delete payload._id;await db.collection('players').doc(ctx.playerKey).update({lastLogin:ctx.state.lastLogin,daily:ctx.state.daily,version:ctx.state.version,configVersion:ctx.state.configVersion});return c.ok({state:game.publicState(ctx.state),identityKind:ctx.kind,linked:ctx.playerKey!==ctx.key||!!ctx.state.profile?.linked});}catch(e){return c.fail(e)}};
