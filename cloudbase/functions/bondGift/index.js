const game=require('./lib/game');const c=require('./lib/cloud');
exports.main=async(event={})=>{try{const heroId=String(event.heroId||''),count=Math.max(1,Math.min(20,Number(event.count)||1)),requestId=String(event.requestId||'').slice(0,80);if(!requestId)throw game.codeError('REQUEST_ID_REQUIRED');const{db,auth}=c.init();const ctx=await c.playerContext(db,auth);const out=await c.optimisticMutate(db,ctx.playerKey,requestId,`bondGift:${heroId}`,state=>game.giftHero(state,heroId,count));return c.ok({state:game.publicState(out.state),bond:out.result,idempotent:out.idempotent});}catch(e){return c.fail(e)}};
