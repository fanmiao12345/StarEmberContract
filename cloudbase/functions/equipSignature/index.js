const game=require('./lib/game');const c=require('./lib/cloud');
exports.main=async(event={})=>{try{const heroId=String(event.heroId||''),signatureId=event.signatureId==null?null:String(event.signatureId),requestId=String(event.requestId||'').slice(0,80);if(!requestId)throw game.codeError('REQUEST_ID_REQUIRED');const{db,auth}=c.init();const ctx=await c.playerContext(db,auth);const out=await c.optimisticMutate(db,ctx.playerKey,requestId,`signature:${heroId}:${signatureId}`,state=>game.equipSignature(state,heroId,signatureId));return c.ok({state:game.publicState(out.state),equip:out.result,idempotent:out.idempotent});}catch(e){return c.fail(e)}};