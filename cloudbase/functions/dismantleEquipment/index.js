const game=require('./lib/game');const c=require('./lib/cloud');
exports.main=async(event={})=>{try{const itemId=String(event.itemId||''),count=Math.max(1,Math.min(99,Number(event.count)||1)),requestId=String(event.requestId||'').slice(0,80);if(!requestId)throw game.codeError('REQUEST_ID_REQUIRED');const{db,auth}=c.init();const ctx=await c.playerContext(db,auth);const out=await c.optimisticMutate(db,ctx.playerKey,requestId,`dismantle:${itemId}`,state=>game.dismantleEquipment(state,itemId,count));return c.ok({state:game.publicState(out.state),dismantle:out.result,idempotent:out.idempotent});}catch(e){return c.fail(e)}};
