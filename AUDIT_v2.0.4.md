# 《星烬契约》v2.0.4 移动端与功能可达性审计

本轮基于当前 Web v2.0.3 代码继续检查，目标不是增加玩法，而是修复“功能存在但玩家难以找到/无法操作”和手机端信息密度问题。

## 已确认并修复的问题

1. **培养系统存在功能但缺少入口**
   - `starUp / awakenHero / equipSignature` 逻辑和云端接口已经存在，但契灵详情页没有对应按钮。
   - 已补齐：升级、升星、觉醒、契印武装四条培养链，并显示成本、当前资源、前置条件和禁用状态。

2. **手机底部导航隐藏了整备/商店/活动，但没有统一替代入口**
   - 原移动端仅保留首页、契灵、契约、阵容、探索，进入二级系统后容易迷路。
   - 新增顶部“更多系统”按钮和底部抽屉：整备、商店、活动、星轨事务所、云存档、设置均可随时进入。

3. **手机首页过长**
   - 邮件、阵营共鸣、每日任务、周任务、成就、账号全部堆在首页。
   - 新增“星轨事务所”，移动端首页隐藏这些长面板，通过事务所集中查看；桌面端仍保留完整信息。

4. **整备页装备数量文案容易误导**
   - 原来只显示“持有 N”，但部分副本可能已被其他角色占用。
   - 改为“可用 X/N”，与装备占用逻辑一致。

5. **云端已配置但玩家可能一直在本地试玩**
   - CloudBase 配置有效时新增“云存档尚未连接”提示条，明确当前本地状态并提供一键连接。
   - 更多系统抽屉也提供云存档连接/刷新入口。

6. **错误信息不完整**
   - 对本地核心逻辑和 CloudBase Bridge 做了错误码覆盖检查。
   - 补齐 `HERO_NOT_OWNED`、`BATTLE_TARGET_REQUIRED`、`INVALID_BATTLE_TARGET`、`CLOUDBASE_SDK_LOAD_FAILED`、`ANONYMOUS_AUTH_UNAVAILABLE` 等缺失中文提示。
   - 当前扫描到的本地错误码均有用户可读文本。

7. **开发者入口暴露在正式试玩页面**
   - “重置本地存档”不再默认出现在探索页。
   - 仅在本地模式并显式使用 `?debug=1` 时显示。

8. **手机顶栏操作可能被资源横向滚动挤走**
   - “更多”和“设置”从资源滚动轨中独立出来，始终可触达。

9. **阵容页延续 v2.0.3 修复**
   - 前后排横滑卡、候选 2 列、5 人满编禁用提示、预设横滑、云端换排不可变提交继续保留。

10. **缓存更新**
   - Service Worker 缓存升级为 `star-ember-v20-mobile4`。
   - HTML/JS/CSS/JSON 网络优先，静态图片缓存优先。

## 自动检查结果

- UI `data-action` 数量：80
- 未绑定操作：0
- 无 UI 入口的操作处理器：0
- 已扫描本地错误码：82
- 缺失用户提示：0
- `npm test`：通过
- `npm run check`：通过
- `npm run release:build`：通过
- 发布资源：75 个，缺失 0

## 云端兼容

- `SAVE_VERSION` 保持 20，不要求玩家存档迁移。
- CloudBase 环境继续使用 `star-ember-d2grc7dhvba07314f`。
- 本轮没有修改 36 个云函数，部署本版本时只需要重新发布 Web 静态文件。
