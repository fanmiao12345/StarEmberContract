# 星烬契约 v2.2.1 Report-Driven Hardening

## 本轮依据

本轮以四份审计/设计材料为输入：`SUGGESTIONS.md`、`TEST_REPORT.md`、`PLAYER_JOURNAL.md`、`AI_UI_PROMPT.md`。目标不是宣称“修完所有 bug”，而是优先处理已经复现的数据安全、核心战斗反馈、移动端交互与设计稿高收益差距。

## 已修复 / 已加固

### 数据与经济
- E1：新手武器回填改为一次性 `rewardBackfills.v003`，不再通过“背包为空”重复发放。
- E3：自动战斗与未完成手动战斗互斥；过期手动会话不能再次发放同关奖励/掉落。
- E4：`migrate()` 对 heroes / formation / inventory / activeBattleSession 等异常类型做修复；空英雄存档回填基础角色。
- E5：成就强化进度过滤非有限值，领取端再次拒绝 NaN/Infinity。
- E2：新增 `lastSeenDay` 单调保护，并取消公开规则 API 依赖外部 Date 参数的重置路径。**本地纯客户端模式仍无法从根本上防止用户持续把系统时钟向未来移动；云模式应以服务端时间作为最终可信时间源。**

### 云端与存档保护
- B1：CloudBase Web SDK 地址从不存在的 3.0.1 固定地址切换到可用 `latest` 地址。
- B2（代码侧）：403 / EXCEED_AUTHORITY 不再伪装成“网络不稳定”，会提示 CloudBase 权限配置问题。**腾讯 CloudBase 控制台侧的 403 权限本轮没有远程代操作，也未验证已解除。**
- B6：本地已有非空进度时连接云端前先生成 `.backup` 快照，并要求用户确认。
- B7：云模式成功操作后把最新云状态镜像到本地存档，降低关闭自动恢复后的“回滚感”。
- B10：自动恢复云存档失败不再静默，启动文案与 toast 会提示当前进入本地保护。
- B11：`saveUi()` 增加容错，localStorage 写失败不会截断导航/教程渲染链。

### 战斗与结算（P0）
- B4：自动战斗不再主动关闭结算；现在先用引擎真实 `timeline/stats/rewards` 播放压缩战斗战报，再进入胜利/失败结算。
- 自动战斗结束会进入“扫荡/记录”战斗画面，真实显示双方 HP、技能文本、伤害/治疗/护盾变化、MVP、战利品与战斗统计。
- Boss 单位优先使用项目现有 Boss 资源，不再只显示字符核心；但原始 Boss 素材仍是占位风格，视觉质量受现有素材限制。
- B5：仅在探索中的进行中手动战斗隐藏底部导航，不再把整个 App 的导航一起隐藏。
- B8：切页和新战斗前清理 replay timer，避免回放跨页面继续重绘和在错误页面弹结算。
- B9：十连/结果弹层统一进入高层模态规则，底层导航不能穿透操作。
- 扫荡从单条 toast 升级为正式“扫荡完成”结果弹层，显示次数、剩余次数、资源和掉落，并支持再次扫荡。

### 移动端交互
- 核心按钮、顶部按钮、关闭按钮、text button 提升到约 44px+ 触控目标。
- 五大一级页面增加固定导航的真实底部占位，活动/事务所末尾内容可滚到导航上方。
- 编队从超长列表改为 5 队位紧凑总览；候选角色列表限高内部滚动。
- “快捷编队”不再错误读取预设1，而是真正从已拥有角色中生成推荐五人和前后排；无变化时给明确反馈。
- toast 改为可换行且 `pointer-events:none`，避免短时间覆盖按钮并抢点击。
- 设置/模态继续使用统一高层 overlay 规则；渲染后补 `role=dialog` / `aria-modal` / close aria-label / active nav aria-current。

### 信息表达与设计稿靠近
- 首页资源抽屉改为资源图标 + 数量 + 来源说明 + 对应入口，不再只有 6 个数字。
- 顶部待办入口从容易和星髓混淆的 `✦` 改为菜单/待办语义图标。
- 探索主线加入当前章节关卡列表：已通关 / 当前 / 锁定、★★★ 通关状态、当前目标详情、当前战力和 Boss 参考战力。
- “推荐战力”在 Boss 确认中改成“参考战力 · 非硬门槛”，避免把它误读成准入线。
- 角色升级加入 `Lv.N → Lv.N+1`、生命/攻击/防御/速度的真实前后数值与增量、持有/所需烬币。
- 烬币不足时提供“前往资源裂境”直达，不再只把升级按钮变灰。
- 抽卡保底文案改为“距保底还差 N 抽”；限定池明确“首次 SSR UP 50%，未中则下次 SSR 必中”，不改变真实概率。
- 迁移码输入使用数字键盘、6 位限制、重渲染保留草稿，并增加复制迁移码。
- 教程 CTA 文字与实际跳转重新对齐，明确它是功能导览而非任务完成。

### PWA / 平台
- B23：首次 pointer/touch 手势尝试创建/恢复 AudioContext，降低 iOS 全程静音概率。
- B24：Service Worker 预缓存改为 best-effort，单个资源 404 不再让整次安装失败；新 SW 不再安装后立即强抢控制权，检测新版本时显示“立即刷新/稍后”。
- `fx-medium` 增加真实降级规则，不再和 high 基本等价。

## 有意未修改
- 抽卡概率、碎片转化、货币收益、关卡奖励等已验证正确的规则没有为了 UI 设计稿而改写。
- 不新增设计稿里的“体力/星轨能量”，因为当前引擎没有该字段。
- E7“给新号补发 v0.8 历史邮件”没有照报告直接实现：v2.1.5 已明确把旧测试补偿从新账号隔离，直接恢复会重新污染新号经济。邮件 UI 仍应后续以新的正式系统邮件策略完善。
- B19 BGM 未伪造：项目当前没有正式 BGM 音频素材。本轮只修复音效解锁，不用振荡器冒充正式音乐。
- B45/B46/B47（自动策略差异、连携触发、中后期数值）属于玩法数值规则调整，本轮 UI/安全修复不擅自改数值。

## 仍待验证 / 外部依赖
- CloudBase B2 403：需要腾讯 CloudBase 控制台核对匿名登录、云函数调用权限、安全规则与域名/网关配置。
- 真实付费没有测试，也没有新增任何真实支付入口。
- 真机 iOS/Android 的软键盘、地址栏收缩、PWA 安装与 AudioContext 仍需要设备验证。
- 本执行环境对本地 Chromium/Playwright 页面访问返回 `ERR_BLOCKED_BY_ADMINISTRATOR`，因此本轮没有用概念图冒充“真实运行截图”；最终视觉需要用户本机或可运行浏览器环境再次截屏验收。
- 敌方/Boss 美术素材不足仍是战斗“像完整手游”的最大视觉缺口之一。

## 自动回归

最终交付前执行：

```text
npm test
npm run check
npm run release:build
```

验证范围包括 core / stability / gameplay / 历史 UI smoke / v2.2.1 modal-nav hardening / release assets，以及 116 个脚本的 shared cloud helper 一致性。
